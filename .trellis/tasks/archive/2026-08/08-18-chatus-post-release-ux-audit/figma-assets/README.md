# Figma Review Icons

These SVGs are derived from the repository's installed `lucide-react@1.24.0` package and retain its ISC license. They are temporary Figma import sources for the post-release review set; production code continues to use `lucide-react` components directly.
